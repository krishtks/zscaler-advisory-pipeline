FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn[standard] httpx python-dotenv
COPY ai_guard_middleware.py .
VOLUME ["/app/logs"]
CMD ["uvicorn", "ai_guard_middleware:app", "--host", "0.0.0.0", "--port", "8000"]
